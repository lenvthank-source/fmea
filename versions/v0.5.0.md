# v0.5.0 — 2026-08-29
## Summary
Transforms public frontend into experience-store with massive new-gen video hero (`Quality Engineered To Evolve` `BubbledotICG-FinePos` + CloudFront `d8j0ntlcm91z4...mp4` `100vh/100dvh` scoped `720px` pill `HeroVideo.tsx`), adds browsable `Product/About/Blog(headless+n8n)/Pricing/LearnHub` (6 new routes, `sitemap.xml` 18 URLs), densifies app ergonomics (bento `48px`, `p:2`, `Fab` unify, font preserved `0.85rem`), and scaffolds preview licensing (`public-try` tenant, `POST /auth/try-token`, cap 10, `RequestIdMiddleware` + `X-Request-Id` logging, `gitignore` allow-list). All existing features kept working (`Hero` hidden not deleted, `tcs` green, `readme_sync`).
## Changes
### Features
- **Store video hero (Q1 A+A1, Q2 A):** new `frontend/src/features/landing/HeroVideo.tsx:1` (186 lines, `--bg:#000 --pill-dark:#28282a` `design_prompt:65`, trust row `clamp(36px,4.5vw,42px)` `fa-industry/car/microchip` `Trusted by 2000+ Enterprises`, headline `Quality`/`Engineered To Evolve` `clamp(28px,6.2vw,80px) -0.04em` `headlineFade 0.85s`, stats `120ms/99.99%/24/7/2.4M` count-up) scoped `100vh/100dvh overflow:hidden` **only on `/`**; old `LandingPage:236 nav` + `hero 305 85vh` + `stats 413` hidden via `display:none` (not deleted). Fonts `Inter 400-600` + `BubbledotICG-FinePos` `db.onlinewebfonts.com` + `Font Awesome 6.5.2` injected, `Geist Pixel` fallback.
- **Experience-store pages (Q3 B headless, Q4 logos):** `frontend/src/features/product/ProductPage.tsx:1` (6 features `PFMEA/DFMEA/PFD↔PFMEA/Control Plan/Actions/21 CFR` + `SPECS 6`), `frontend/src/features/company/AboutPage.tsx:1` (mission + 8 manufacturing/auto logos `bosch/magna/continental...` placeholder `Avatar`, no team names), `frontend/src/features/pricing/PricingPage.tsx:1` (Preview $0 10 projects / Team $49 / Enterprise custom `License JWT RS256`), `frontend/src/features/learn/LearnHubPage.tsx:1` (4 pillars), `frontend/src/features/blog/BlogListPage.tsx:1` (`GET /cms/blog/posts` headless, `n8n POST /cms/blog/publish` ready, fallback 3 posts, `BlogPosting` `SEO:71`), `frontend/src/features/blog/BlogPostPage.tsx:1` (`/:slug`), `frontend/src/app/router.tsx:40` add `/product /about /pricing /learn /blog /blog/:slug` (keep `/learn/:slug` pillar). `frontend/public/sitemap.xml:1` expanded 2→18 URLs (`/product /about /pricing /learn /blog` + 4 pillars + 4 industries + 2 vs + 2 glossary).
### Fixes
- **Preview tenant + cap (1a):** `backend/src/prisma/prisma.service.ts:36` add `ensurePublicTryTenant()` (seed `public-try` `Admin` all 22 perms + `preview-admin@public-try.local`, upsert if `tenantCount>0`), `backend/src/modules/auth/auth.controller.ts:31` `POST /auth/try-token @Public` → `auth.service.ts:533 createTryToken()` (find `public-try` tenant, `Admin` role, `generateTokens` `isGuest:false`), `backend/src/modules/project/project.service.ts:83` preview cap `public-try` `count>=10 → BadRequest`. Frontend `HeroVideo:CTA` `guestLogin` still works, `try-token` ready for `VITE_EDITION=try` auto-fetch.
### Refactors
- **Ergonomics densify (Q5 A-font-preserved, no break):** `frontend/src/index.css:10` `#F7F6F2→#F8FAFC` (`theme.ts:19`), `frontend/src/components/Layout/AppShell.tsx:443` `p:3 24px→p:2 16px` (bento), `frontend/src/features/landing/LandingPage.tsx:277` remove `ThemeToggle` (dead `ColorModeContext:5 light`), `docs/ERGONOMICS.md:1` bento `48px` + font `0.85rem 13.6px` preserved + `p:2` + Fab unify + column group spec. Hidden hero keeps `typewriter`/`counters` but off-screen (no layout shift).
- **Observability baseline:** new `backend/src/common/request-id.middleware.ts:1` (`X-Request-Id` `randomUUID` + header), `backend/src/app.module.ts:1` `NestModule configure` `RequestIdMiddleware forRoutes '*'`, `backend/src/common/global-exception.filter.ts:40` log `[requestId][tenantId] method url - status - message` + `response.setHeader X-Request-Id` + `json.requestId`, `backend/src/main.ts:22` `allowedHeaders X-Request-Id`, `frontend/src/features/landing/LandingPage.tsx:236` hidden nav comment.
- **Git tracking:** `.gitignore:36` add `!.agents/AGENTS.md etc.`, `!.agents/agents/** context/** memory/** rules/** workflows/** skills/** scripts/** prompts/** checklists/**`, `!/package.json !fmea-pod.yaml !docker-compose*.yaml !/deployment_guide.md` + `!/docs/ERGONOMICS.md` (fix `0.4.0` force-add debt).
## Files Touched
- `.gitignore:36,39,41,49` — allow-list for mesh + compose + ERGONOMICS
- `frontend/src/index.css:10` — `#F7F6F2→#F8FAFC`
- `backend/src/common/request-id.middleware.ts:1` — new `X-Request-Id`
- `backend/src/app.module.ts:1,27` — `MiddlewareConsumer` + `RequestIdMiddleware`
- `backend/src/common/global-exception.filter.ts:40` — `requestId+tenantId` log + header
- `backend/src/main.ts:22` — `allowedHeaders X-Request-Id`
- `backend/src/prisma/prisma.service.ts:36,172` — `ensurePublicTryTenant()` public-try seed
- `backend/src/modules/auth/auth.controller.ts:31` — `POST /auth/try-token`
- `backend/src/modules/auth/auth.service.ts:644` — `createTryToken()` `Admin` JWT
- `backend/src/modules/project/project.service.ts:83` — preview cap 10
- `frontend/src/features/landing/HeroVideo.tsx:1` — new 186-line video hero (Quality Engineered To Evolve)
- `frontend/src/features/landing/LandingPage.tsx:5,227,278,305,413` — `HeroVideo` import + `display:none` old hero/nav/stats + `ThemeToggle` removed
- `frontend/src/features/product/ProductPage.tsx:1` — new
- `frontend/src/features/company/AboutPage.tsx:1` — new 8 logos
- `frontend/src/features/pricing/PricingPage.tsx:1` — new 3 tiers (fix `}}` 34)
- `frontend/src/features/learn/LearnHubPage.tsx:1` — new
- `frontend/src/features/blog/BlogListPage.tsx:1` — new headless fallback
- `frontend/src/features/blog/BlogPostPage.tsx:1` — new
- `frontend/src/app/router.tsx:8,49` — 6 new routes + imports
- `frontend/public/sitemap.xml:1` — 2→18 URLs
- `docs/ERGONOMICS.md:1` — new densification spec
- `frontend/src/components/Layout/AppShell.tsx:443` — `p:3→p:2`
- `package.json:3` — version `0.5.0`
- `backend/package.json:3` — version `0.5.0`
- `frontend/package.json:4` — version `0.5.0`
- `versions/v0.5.0.md:1` — this ledger
## Schema/Migrations
- None (public-try tenant seeded via `seedDefaultData` `ensurePublicTryTenant`, no schema change; `project` cap is code gate). On next `prisma db push` or Render deploy, `public-try` tenant appears if `tenantCount>0` but `public-try` missing.
## Breaking / Notes
- `Hero`/`nav`/`stats` hidden not deleted → zero break; `LandingPage` still holds `typewriter`/`counters` but off-screen. `HeroVideo` loads `BubbledotICG-FinePos` + `Font Awesome 6.5.2` via CDN inject; fallback `Geist Pixel` at `/fonts/GeistPixel-Circle.woff2` if missing, graceful.
- `POST /auth/try-token` is `@Public` — preview Admin token (full perms) — only for `public-try` subdomain; not for production tenants. `project cap 10` only on `subdomain==='public-try'`.
- `Blog` headless endpoint `/cms/blog/*` not yet implemented backend — `BlogListPage` falls back to 3 static posts until `n8n→POST /cms/blog/publish` exists.
- `X-Request-Id` now echoed in every error JSON and header for IT dashboard `Fleet|Health|Logs` (next `v1.0-enterprise`).
- No `DATABASE_URL` change; preview Neon branch to be created as `DATABASE_URL_try` (next step).
